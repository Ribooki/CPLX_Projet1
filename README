Pour compiler le programme principal :
	make
Pour lancer le programme :
	./main fichier
Pour lancer le programme avec un fichier par défaut :
	make run

Pour compiler le test de la question 1 :
	make testV
Pour lancer :
	./testV fichier

Pour compiler le test de la question 2 :
	make testM
Pour lancer :
	./testM fichier

Pour compiler le test de desertToSAT  :
	make testDesertToSAT
Pour lancer :
	./testDesertToSAT fichier
